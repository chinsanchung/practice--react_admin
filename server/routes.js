const SHOPS = "/Shops";
const MEMBERS = "/Members";
const EVENTS = "/Events";
const ANNOUNCES = "/Announces";
const ID_URL = "/:id";

const routes = {
  shops: SHOPS,
  members: MEMBERS,
  events: EVENTS,
  announces: ANNOUNCES,
  id_url: ID_URL,
};

export default routes;
